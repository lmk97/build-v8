name: C API for V8 monolithic static library
on:
   [push]
#  release:
#    types:
#      - published

env:
  V8_VERSION: '15.2.124.1'

defaults:
  run:
    shell: bash

jobs:
  build-on-linux-aarch64:
    runs-on: ubuntu-22.04
    env:
      V8_OS: linux
      V8_ARCH: aarch64
    steps:
      - name: Source code checkout
        uses: actions/checkout@v7
        with:
          path: ${{ github.event.repository.name }}
      - name: V8 fetch and build
        run: |
          git clone https://chromium.googlesource.com/chromium/tools/depot_tools.git
          export DEPOT_TOOLS_METRICS=0
          export PATH="$PATH:$GITHUB_WORKSPACE/depot_tools"
          gclient
          fetch v8
          cd $GITHUB_WORKSPACE/v8
          git checkout $V8_VERSION
          gclient sync
          mkdir "target-$V8_OS-$V8_ARCH"
          cp "../${{ github.event.repository.name }}/$V8_OS-$V8_ARCH.gn" "target-$V8_OS-$V8_ARCH/args.gn"
          gn gen "target-$V8_OS-$V8_ARCH"
          ninja -j 4 v8_monolith -C "target-$V8_OS-$V8_ARCH"
          echo "---------------------- file    ----------------------"
          file "target-$V8_OS-$V8_ARCH/obj/libv8_monolith.a"
          echo "---------------------- readelf ----------------------"
          readelf -h "target-$V8_OS-$V8_ARCH/obj/libv8_monolith.a"
          echo "---------------------- objdump ----------------------"
          objdump -f "target-$V8_OS-$V8_ARCH/obj/libv8_monolith.a" | grep architecture
